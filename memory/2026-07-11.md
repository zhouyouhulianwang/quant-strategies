# 2026-07-11 工作记录

## 行业映射配置外置（P1优化）
- **时间**: 2026-07-11 UTC
- **操作**: 将 `_BuildSectorMap()` 中的硬编码行业映射提取到外部 `sector_map.json` 配置文件
- **文件**: `sector_map.json`（255只股票 → 10个行业）
- **代码修改**: `main.py` 中 `_BuildSectorMap()` 改为多路径配置文件加载，保留硬编码 fallback
- **加载优先级**: 算法目录 → 当前工作目录 → 绝对路径 → 上级目录 → 硬编码
- **GitHub Commit**: `54731b6`
- **影响**: 精简 `main.py` 代码体积，避免 64KB 限制，提高云端部署灵活性

## 当前状态
- **行业映射**: 配置外置完成 ✅
- **股票池**: 完全从 `stock_pools.json` 加载（硬编码已移除）✅
- **本地回测**: 通过（需验证最新版本）
- **云端回测**: 被节点占用阻塞 ❌
- **最佳历史表现**: 489% 收益, 20.7% 回撤, 1.225 Sharpe, 294 笔交易

## 待完成
- [ ] 云端回测验证当前版本（1e1df84 + 行业映射外置 + 调度优化 + 止损优化）
- [ ] 对比历史最佳结果（489%）
- [ ] 如果表现良好，准备实盘部署
- [ ] 解决云端节点占用问题（停止历史回测或添加专用节点）


## 移除股票池，sector_map 作为唯一交易宇宙（2026-07-11 UTC）
- **操作**: 删除 `stock_pools.json`，`sector_map` 直接作为交易股票列表；`us_tickers = list(self.sector_map.keys())`
- **代码修改**: 移除 `main.py` 中 `_GetUSStockPool()`、`UpdateStockPool()`、`stock_pool_source`、`stock_pool_file`、`enable_dynamic_pool` 及动态调度
- **文件清理**: 删除旧 `stock_pools.json`、`sector_map.json` 重复文件
- **GitHub Commit**: 推送到 `zhouyouhulianwang/quant-strategies` master
- **影响**: 消除股票池与行业映射的双重维护，单一数据源减少错误

## 补齐缺失数据并创建 Lean map_files
- **操作**: 下载 Yahoo Finance 数据约 54 只缺失 ticker，生成 674 个 `map_files/` CSV； Lean 忽略无 map_file 的 zip，因此必须成对创建
- **不可用的 ticker 已移除（Yahoo 无数据）**: HOLX, HES, BK, WBA, SQ, PARA, MMC, K, IPG, FI, DAY, CMA（12 只）
- **已收购/退市移除**: ANSS, PXD, JNPR, SPLK, MRO, WRK（6 只）
- **最终 universe**: 464 只 ticker
- **教训**: Lean 回测时 "Data unavailable" 通常是因为缺少 `map_files/{ticker}.csv`，而非 zip 缺失

## 回测结果（464 只股票， cleaned code）
- **净收益**: 347.53%
- **年化收益 (CAGR)**: 25.92%
- **最大回撤**: 25.00%
- **夏普比率**: 0.915
- **索提诺比率**: 1.036
- **胜率**: 89% (52 胜 / 6 负)
- **平均盈利**: 9.85%
- **平均亏损**: -8.19%
- **总佣金**: $197.25
- **交易笔数**: 58
- **数据加载**: 469/821 成功（失败主要为 VIX 指数不可用，策略已回退到 VIXY）

## 代码清理（main.py）
- 删除所有 `P0优化` / `P0修复` / `v3.1` 等开发过程标记
- 删除硬编码行业 fallback `['Tech', 'Finance', 'Healthcare']`
- 删除未使用的 `deque` 导入、`max_drawdown_pct` 变量
- 删除过时的箭头注释（如 `50%→80%`）和佣金估算注释
- 删除多余空行，保持代码整洁
- **验证**: 清理前后回测结果完全一致，未引入 regression

## 关键文件位置
- **策略代码**: `/home/pc/.openclaw/workspace/quantconnect-projects/AdaptiveMomentumV3_1/main.py`
- **配置文件**: `/home/pc/.openclaw/workspace/quantconnect-projects/strategy_config.json`（464 tickers, 10 sectors）
- **Lean 数据**: `/home/pc/.openclaw/workspace/quantconnect-projects/data/equity/usa/daily/`（690 zips） + `map_files/`（700 entries）
- **GitHub**: `github.com:zhouyouhulianwang/quant-strategies.git` master
