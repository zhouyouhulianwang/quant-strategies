
## 11:09 UTC - User preference: always use real data for backtests
- Dave explicitly requested: 以后回测必须使用真实数据（QuantConnect），不要用 mock data。
- 已记录到 `MEMORY.md` 长期记忆。
- 后续回测默认使用 `python3 run_strategy.py --backtest`（真实数据路径）。

## 11:22 UTC - Pre-compaction memory flush: comprehensive audit completed
- 用户 11:10 UTC 要求：基于量化交易最佳实践，从框架、代码、逻辑、风控、日志、配置全面检查 Alpaca 纸/实盘缺陷。
- 已派两个子代理并行审计并完成：
  - `AUDIT_REPORT_ALPACA_PAPER_LIVE.md`（Alpaca 执行路径）
  - `AUDIT_REPORT_FRAMEWORK_RISK_DATA.md`（框架/数据/风控/日志/配置）
- 缺陷汇总：**P0 16 项，P1 31 项，P2 20 项**。
- 关键 P0 风险（阻塞实盘）：
  - V14Strategy `__init__` 在 `config=None` 时崩溃。
  - VIX 回落 / 盘中监控自动恢复交易，无需人工确认。
  - `config.json` 中 `rebalance_frequency=daily` 与策略文档/示例不一致，容易误高频交易。
  - V14Strategy 风控监控器未正确关联到执行器，风险触发后无法阻止在途订单。
  - `paper` 标志与 `base_url` 未强制一致，可能误用实盘。
  - PDT tracker 将未知建仓日期持仓默认记为今日，误判 day trade。
  - 部分成交补单前未撤销原订单，可能重复成交/超配。
  - 数据清洗对停牌/退市股票无限 `ffill()`，存在前视/幸存者偏差。
  - `setup_logging()` 在 10+ 模块导入期被调用，日志重复输出。
  - 缺少 `HEARTBEAT.md` 日常巡检清单。
- 我已向 Dave 汇报结果，并询问是否按优先级开始修复 P0。
- 后续如进入修复阶段，应先止血（P0），再做 P1，最后 P2；修复后需完整 mock 端到端测试 + 真实数据回测交叉验证。

## 11:43 UTC - P0 修复完成并推送
- 用户 11:22 UTC 回复“立即修复”后，已立即实施 P0 级修复。
- 三个子代理并行修复：框架/风控/日志、Alpaca/PDT/订单、数据/回测。
- 修复后全量测试：`python3 -m pytest test_suite.py -q` → **88 passed, 1 warning**（warning 是 Python 3.14 tarfile 弃用提示，无关）。
- 提交并推送：`df3ede2` — `fix(p0-audit): 实施 P0 级 Alpaca/框架/风控/数据修复` → `github.com:zhouyouhulianwang/quant-strategies.git`。
- 已修复的核心 P0 问题：
  - V14Strategy `config=None` 时初始化崩溃。
  - VIX 回落 / 盘中监控自动恢复交易已移除。
  - 风险监控器先于执行器创建并正确关联；`rebalance_portfolio` 入口统一检查 `trading_halted`。
  - `paper` 与 `base_url` 不一致时初始化报错，防止误用实盘。
  - PDT 未知建仓日持仓不再被误判为今日建仓。
  - 部分成交补单前撤销原订单；订单提交增加 3 次指数退避重试。
  - 数据前向填充限制最大 5 个交易日；退市标的不填充。
  - V14Strategy 回测统一复用 `main.run_v14` 的真实成交/成本引擎。
  - 移除所有模块导入期的 `setup_logging()`，消除日志重复。
  - 新建 `HEARTBEAT.md` 日常巡检清单。
- 手动修改 `config.json` 的 `rebalance_frequency` 从 `daily` 改为 `monthly`，但因此文件在 `.gitignore` 中，未提交到仓库。
- 已向 Dave 报告完成状态，并询问是否继续修 P1 或先跑纸交易 smoke test。
- 后续如继续修复，仍按 P0 → P1 → P2 顺序；P1 中仍包括 CLI `--paper` 实际跑回测、`paper_smoke_test.py` 的 `--live` 仍走纸交易、累计回撤阈值硬编码、.env 注入等关键问题。

## 11:55 UTC - 配置参数验证与修复
- 用户 11:45 UTC 要求：修改 `config.json` 参数，验证一致性和有效性，验证后改回原来参数。
- 我临时把 `config.json` 改为多个无效值进行验证：
  - `rebalance_frequency` → `hourly`（非法）
  - `max_position_pct` → `1.5`（超出 1）
  - `check_interval` → `5`（小于 10）
  - `alpaca_base_url` → `https://wrong.com`（非法域名）
- 验证结果：
  - `rebalance_frequency`、`max_position_pct`、`check_interval` 的非法值均被 Pydantic 验证器正确拦截。
  - **发现严重 bug**：`alpaca_base_url` 在 `config.py` 中无法从 `config.json` 读取，无论配置写什么，始终使用默认值 `https://paper-api.alpaca.markets`。这意味着实盘 URL 配置无效，会永远走纸交易地址。
- 已修复 `config.py`：
  - 移除导致输入被忽略的 `mode='before'` 字段验证器 `_load_base_url_from_env`。
  - 在 `get_config()` 中统一处理 `ALPACA_BASE_URL` 环境变量覆盖。
  - 保留 URL 白名校验（仅允许 Alpaca 两个官方地址）。
- 已把 `config.json` 改回原始有效参数：`rebalance_frequency=monthly`、`max_position_pct=0.2`、`check_interval=60`、`alpaca_base_url=https://paper-api.alpaca.markets`。
- 修复后测试：`python3 -m pytest test_suite.py -q` → **88 passed, 1 warning**。
- 提交并推送：`d3545c0` — `fix(config): 修复 alpaca_base_url 无法从 config.json 读取的 bug` → `github.com:zhouyouhulianwang/quant-strategies.git`。
- 教训：配置验证器不仅要存在，还要实际生效；以后修改配置参数后必须验证运行时是否真正读取到新值，而不是只看默认无报错。

## 12:15 UTC - 用户要求修复剩余 P1/P2 缺陷
- 用户 11:55 UTC 要求：修复剩余的 bug。
- 已启动四个子代理并行处理剩余 P1/P2 缺陷：
  1. `p1_cli_execution_config_fixes`：CLI/执行路径/配置
  2. `p1_data_scheduler_cache_fixes`：数据/调度/缓存
  3. `p1_risk_ops_observability_fixes`：风控/运维/可观测性
  4. `p1_p2_order_observability_reconcile`：订单状态机/生命周期日志/对账
- 其中 `p1_p2_order_observability_reconcile` 子代理执行失败，已告知用户，需要后续检查失败原因并重新启动或手动处理。
- 其余三个子代理仍在运行中。
- 待所有子代理完成后，需统一运行 `pytest test_suite.py -q` 验证并合并提交。

## 12:22 UTC - CLI/执行路径/配置子代理完成
- `p1_cli_execution_config_fixes` 子代理已完成并返回报告。
- 修改文件：`run_strategy.py`、`paper_smoke_test.py`、`config.py`、`smoke_test_paper.py`、`alpaca_executor.py`、`test_suite.py`。
- 测试：该子代理报告 **141 passed, 1 warning**。
- 主要修复：
  - `--paper` / `--live` / `--backtest` / `--mock` 四者互斥，避免模式冲突。
  - `--paper` 和 `--live` 真正连接 Alpaca，不再走回测。
  - `--live` 必须带 `--confirm-live`，并强制 `base_url=https://api.alpaca.markets`。
  - `config.json` 读取/解析失败时直接抛异常，不再静默回退。
  - 新增 `smoke_test_paper.py`（默认跳过，仅 `RUN_PAPER_SMOKE=1` 时执行）。
  - 移除 `.env` 全局注入，改为白名单读取。
- 其余三个子代理（`p1_data_scheduler_cache_fixes`、`p1_risk_ops_observability_fixes`、`p1_p2_order_observability_reconcile`）仍在运行中；其中 `p1_p2_order_observability_reconcile` 已报告失败，需后续处理。
- 待所有子代理完成后，统一运行 `pytest test_suite.py -q` 验证并合并提交。

## 12:43 UTC - 预压缩内存刷新
- 无新的用户请求。
- 上述修复状态已记录；等待子代理继续返回结果。
