# V14 MultiFactor Strategy 依赖

# 数据获取
yfinance>=0.2.0
pandas>=1.5.0
numpy>=1.24.0
requests>=2.31.0
pyarrow>=12.0.0

# Alpaca 交易与数据（alpaca-py 新版 SDK，已替代旧版 alpaca-trade-api）
alpaca-py>=0.30.0

# 其他
python-dotenv>=1.0.0

# 备份加密 (AES-GCM via Fernet)
cryptography>=41.0.0
